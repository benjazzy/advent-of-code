pub fn process(input: &str) -> String {
    unimplemented!();
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_process() {
        todo!("Build test");
        let input = "";
        assert_eq!("", process(input));
    }
}
